import React, {PropTypes} from 'react';

const CarrierListRow = (props) => {
    return (
        <div></div>
    );
}

CarrierListRow.propTypes = {
    myProp: PropTypes.string.isRequired
};

export default CarrierListRow;
